OPENAI_API_KEY=
# Optional: choose another Responses API-compatible model.
OPENAI_MODEL=gpt-4.1-mini

# Optional cloud project persistence API scaffold.
AWS_REGION=
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
PROJECTS_TABLE_NAME=
PROJECTS_OWNER_ID=

# Optional Cognito email OTP auth shell (public client config; no secrets).
# User pool app client must support Cognito built-in USER_AUTH with EMAIL_OTP passwordless sign-in.
NEXT_PUBLIC_COGNITO_USER_POOL_ID=
NEXT_PUBLIC_COGNITO_APP_CLIENT_ID=
NEXT_PUBLIC_COGNITO_REGION=
